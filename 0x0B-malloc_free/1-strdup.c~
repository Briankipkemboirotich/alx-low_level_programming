#include "main.h"
#include <stdio.h>

/**
 * *_strdup - returns pointer to new allocatedspace
 *@str: string
 *Return: pointer to array or null
 */

char *_strdup(char *str)
{
	char *strDup;
	int i, j;

	if (str == NULL)
		return (NU:
